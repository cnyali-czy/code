skydogli是一个特务，负责地下联络工作。
联络网络共有$n$个节点，编号为$1$到$n$。网络中存在若干对节点可以直接联络，而其他的节点不能直接联络，只能通过整个网络间接联络。
为了保护$1$号节点的绝对安全，与$1$号节点能直接联络的节点不能超过$k$个，但其他节点没有要求。同时，整个联络网必须保证任意两个节点都能直接或间接联络。
skydogli突发奇想：整个联络网络的构造有几种不同的方案呢？
我们称两个方案是不同的，当且仅当存在存在两个节点在一个方案中能
skydogli这么神当然知道啦。但他想考考你，你只需要回答答案对$998244353$取模的结果即可。

# 输入格式
从$count.in$中读入数据。
一行两个整数$n,k$

# 输出格式
输出到$count.out$中。
一行一个整数表示答案。
